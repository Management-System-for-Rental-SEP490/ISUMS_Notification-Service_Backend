package com.isums.notificationservice.domains.enums;

/**
 * Locale codes used as the template folder suffix, e.g. templates are
 * named {baseKey}_vi_VN, {baseKey}_en_US, {baseKey}_ja_JP.
 */
public enum LocaleType {
    vi_VN,
    en_US,
    ja_JP
}
